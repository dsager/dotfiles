# ~/.tmuxinator/devex.yml

name: devex
on_project_start: cd ~/src/work/docker-setup
windows:
  - devex:
      layout: even-vertical
      panes:
        - cd grape-api
        - docker-compose logs --tail=50 -f
        - watch docker-compose ps
