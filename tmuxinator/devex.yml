# ~/.tmuxinator/devex.yml

name: devex
on_project_start: cd ~/src/work/docker-setup && docker-compose up -d
windows:
  - docker:
      layout: tiled
      panes:
        - 
        - watch docker-compose ps
        - docker-compose logs --tail=50 -f
  - grape:
    - cd grape-api
  - front-end:
    - cd front-end
